<li id="comment-<%= comment._id %>">
    <p>
        <% if((locals.user) && (locals.user.id == comment.user.id||post.user.id == locals.user.id)) {%>
            <small>
              <a class="delete-comment-button" href="/comments/destroy/<%= comment.id %>"><i class="far fa-trash-alt"></i></a>
            </small>
            <%}%> 
        <span id="displayed-comment"><%= comment.content %></span>
        <br />
        <small id="displayed-comment-writer"> <a href="/users/profile/<%= comment.user.id %>"><%= comment.user.name %></a> </small>
            <br>
        <small>
          <% if(locals.user){%>
            <a class="toggle-like-button" data-likes="<%= comment.likes.length %>" href="/likes/toggle/?id=<%=comment._id%>&type=Comment">
              <%= comment.likes.length %> <span class="like"><i class="fas fa-heart"></i></span>
            </a>
            <% }else{ %>
              <%= comment.likes.length %> <span class="like"><i class="fas fa-heart"></i></span>
          <% } %>
        </small>
      </p>
</li>